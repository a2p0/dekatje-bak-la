<div class="<%= card_classes %>">
  <% if header? %>
    <div class="px-5 py-4">
      <%= header %>
    </div>
  <% end %>

  <% if body? %>
    <div class="px-5 py-4">
      <%= body %>
    </div>
  <% end %>

  <% if footer? %>
    <div class="border-t border-rad-rule px-5 py-4">
      <%= footer %>
    </div>
  <% end %>
</div>
